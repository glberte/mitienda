const MapExample = () => {
    const users = [
        {
            id: 1,
            name: "Roberto",
            age: 40,
        },
        {
            id: 2,
            name: "Jorge",
            age: 40,
        }
    ];
    const usersMapped = users.map( (users) => {} )
    console.log
    return ({...users, name: user.name.toUpperCase()});
    return <div></div>;
}

export default MapExample;